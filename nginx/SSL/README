Generate selsigned files:
sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout nginx-selfsigned.key -out nginx-selfsigned.crt


Generate dhparam.pem:
sudo openssl dhparam -out dhparam.pem 4096
